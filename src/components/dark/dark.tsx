import React, { FC } from 'react'

interface IDark {
  dark: Boolean
  setDark: (lng: Boolean) => void
}
const Dark: FC<IDark> = ({ setDark, dark }) => {
  return (
    <>
    </>
  )
}
export default Dark
